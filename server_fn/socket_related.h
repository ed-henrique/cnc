// Returns new_port as int.
int change_port_server(char* new_port);

// Binds server to socket using HOST for ip and PORT for port, returning the socket.
int connect_socket_server(char* host, int port);
